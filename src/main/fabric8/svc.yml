metadata:
  labels:
    expose: true
  annotations:
    # the default API path when opening this service in the console
    api.service.kubernetes.io/path: "/api/chant"
    # lets expose the swagger
    # TODO the fabric8-maven-plugin should generate these!
    api.kubernetes.io/description-path: "/swagger.json"
    api.kubernetes.io/description-language: "SwaggerJSON"